#include <iostream>

int main()
{
    std::cout << "Hello, World!" << std::flush;

    /*
    In this code, "Hello, World!" is written to the output buffer
    and then immediately sent to the console due to the use of std::flush.
    */
}